import { styled } from '@linaria/react';

import { P } from '~components/Typography';

export const TOCWrapper = styled(P)`
	display: flex;
	flex-flow: row wrap;
	justify-content: center;
	align-items: center;
	gap: 8px;
	width: 100%;
`;
